name: check

# On every PR: lint + a read-only --check --diff against the server, posted to the PR.
# This is the human's review surface (plan §5.1). It NEVER applies.

on:
  pull_request:
    branches: [main]
  workflow_dispatch: {}

concurrency:
  group: check-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: write   # to post the diff as a comment

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install Ansible + lint
        run: pip install ansible-core ansible-lint
      - run: ansible-galaxy collection install -r requirements.yml
      - run: ansible-lint
      - name: Test recoverable AI DevOps toolkit cutover
        run: bash tests/test-ai-devops-toolkit.sh
      - name: Test Ubuntu checkout ownership repair
        run: bash tests/test-dev-ubuntu-permissions.sh
      - name: Syntax check
        run: ansible-playbook playbooks/site.yml --syntax-check

  check-diff:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install ansible-core
      - run: ansible-galaxy collection install -r requirements.yml

      # Join the tailnet with an EPHEMERAL, tagged node so dead CI runners self-remove (§5.1).
      - name: Connect to Tailscale (ephemeral tag:ci)
        uses: tailscale/github-action@v3
        with:
          oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TS_OAUTH_SECRET }}
          tags: tag:ci

      # Pull secrets from 1Password (the SA token is the ONLY secret stored in GitHub, §5.2).
      - name: Load secrets from 1Password
        uses: 1password/load-secrets-action@v2
        with:
          export-env: true
        env:
          OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
          SSH_PRIVATE_KEY: op://vibe_coding/ci-deploy-ssh/private_key

      - name: Install SSH key
        run: |
          mkdir -p ~/.ssh && chmod 700 ~/.ssh
          echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keygen -y -f ~/.ssh/id_ed25519 >/dev/null && echo "ssh key OK" || { echo "SSH KEY INVALID"; exit 1; }

      # Phase 1 only. --check fidelity is weak for phase2 risky roles (§4a), so we don't run
      # them here — they are proven on the scratch host, not via prod --check.
      - name: Dry-run (--check --diff) phase1 against hetzner
        id: diff
        run: |
          set -o pipefail
          ansible-playbook playbooks/site.yml -l hetzner --tags phase1 \
            --check --diff 2>&1 | tee /tmp/diff.txt

      - name: Post diff to PR
        if: always() && github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const body = '### Ansible `--check --diff` (phase1, read-only)\n\n```\n' +
              fs.readFileSync('/tmp/diff.txt','utf8').slice(0, 60000) + '\n```';
            await github.rest.issues.createComment({
              owner: context.repo.owner, repo: context.repo.repo,
              issue_number: context.issue.number, body });

      - name: Publish manual diff summary
        if: always() && github.event_name == 'workflow_dispatch'
        run: |
          {
            echo '### Ansible `--check --diff` (phase1, read-only)'
            echo
            echo '```'
            if [[ -f /tmp/diff.txt ]]; then head -c 60000 /tmp/diff.txt; else echo 'No diff output was produced.'; fi
            echo
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"
