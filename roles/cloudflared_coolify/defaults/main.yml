---
cloudflared_service_name: "cloudflared-coolify"
cloudflared_env_path: "/etc/cloudflared/coolify-tunnel.env"
# Secret — injected at apply time from op://vibe_coding/cf-tunnel-coolify. Never set in git.
cloudflared_tunnel_token: ""
# Deploy the unit/env/reload without enabling the tunnel (scratch testing). Prod = false.
cloudflared_deploy_only: false
