---
# packages — install the FULL set of manually-installed host packages so a rebuild reproduces the
# exact apt footprint (gap R1/R6). The list (files/apt-manual.txt) is captured verbatim from the
# box by bin/discover-software.sh. Additive + idempotent: on the live box every package is already
# present (no-op). Requires apt_repos to have configured the vendor repos first.
#
# RESILIENT: a rebuild must not hard-fail because one package is unavailable in the configured
# repos (e.g. a provider-specific package like hc-utils on a non-Hetzner box, or a package later
# pulled from upstream). So we install only what apt can resolve and LOUDLY report the rest for the
# human to handle. On a same-provider Hetzner rebuild everything resolves.

- name: Load the captured manual apt package list
  ansible.builtin.set_fact:
    packages_apt_manual: >-
      {{ lookup('ansible.builtin.file', 'apt-manual.txt').splitlines()
         | map('trim') | reject('match', '^#') | select | list }}

- name: Determine which manual packages are available in the configured repos
  ansible.builtin.shell:
    cmd: "for p in {{ packages_apt_manual | join(' ') }}; do apt-cache show \"$p\" >/dev/null 2>&1 && echo \"$p\"; done"
  register: packages_available
  changed_when: false
  check_mode: false

- name: Report manual packages NOT available in any configured repo (handle manually after rebuild)
  ansible.builtin.debug:
    msg: "UNAVAILABLE (skipped, not in any configured repo): {{ packages_apt_manual | difference(packages_available.stdout_lines) }}"
  when: (packages_apt_manual | difference(packages_available.stdout_lines)) | length > 0

- name: Install all available manual apt packages (full host package set)
  ansible.builtin.apt:
    name: "{{ packages_available.stdout_lines }}"
    state: present
    update_cache: true
    cache_valid_time: 3600
