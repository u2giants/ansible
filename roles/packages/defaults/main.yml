---
# The full manual apt package set lives in files/apt-manual.txt (captured verbatim from the box
# by bin/discover-software.sh). Regenerate it when packages are intentionally added/removed:
#   ssh vps 'sudo bash -s' < bin/discover-software.sh | grep '^APT ' | sed 's/^APT  //' \
#     | sort -u > roles/packages/files/apt-manual.txt
# (and update docs/baseline-software.txt so the software-drift check stays in sync).
