---
docker_manage_daemon_json: true
docker_live_restore: true        # MUST stay true; and daemon.json MUST NOT contain a dns key
docker_ce_version: ""            # set to the installed version string to hold the package
docker_auto_restart: false       # MUST stay false — never auto-restart Docker
