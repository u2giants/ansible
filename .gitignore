# --- Secrets must never be committed (see README §Secrets and plan §3.5) ---
*.vault
vault.yml
vault.yaml
*vault-password*
.vault_pass
*.pem
*.key
id_rsa*
id_ed25519*
.netrc
.env
.env.*
!.env.example
*.bak
*.bak-*
op-*.json
1password-*.json

# Cloud-init renders containing a real Tailscale auth key
files/cloud-init/*.rendered
files/cloud-init/user-data.yaml

# --- Discovery output (live host internals: ports, iptables, configs) — keep local ---
discovery/

# --- Ansible runtime cruft ---
*.retry
.ansible/
fact_cache/
*.log

# --- Editor / OS ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
